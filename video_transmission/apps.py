from django.apps import AppConfig


class VideoTransmissionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'video_transmission'
    verbose_name = 'Video Transmission'
